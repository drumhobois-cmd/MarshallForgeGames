# Active tasks

Keep exactly one implementation task active unless the user explicitly approves independent parallel work.

Rules:

1. Copy `../TASK_TEMPLATE.md` into this directory and name it `BE-####_SHORT_TITLE.md`.
2. Resolve scope and acceptance criteria before source changes.
3. Assign one implementer and one reviewer; do not let both edit the same files concurrently.
4. Update the task as evidence arrives.
5. Move accepted tasks to a future `tasks/completed/` directory rather than deleting their history.

The initial documentation-only task, `BE-0001_REBASELINE_PROJECT.md`, was accepted on 2026-08-19 and is archived under `../completed/`.

Current task: `BE-0002_INSTRUMENT_JAB_CONTACT.md`.
